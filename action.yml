name: 'Conditional Auto Approve Deployment Action'
description: 'Conditional Auto Approve Deployment requests pending in the github workflows'
author: 'Shiba Prasad Swain'

inputs:
  gh-token:
    description: 'The GitHub token for authentication.'
    required: true
  label:
    description: 'The label to be applied to the pull request.'
    required: true

branding:
  icon: 'activity'
  color: 'green'

runs:
  using: 'node20'
  main: 'dist/index.js'